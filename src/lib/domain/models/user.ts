class User {
  id: number
  name: string
  surname: string

  constructor(id: number, name: string, sur: string) {
    this.id = id
    this.name = name
    this.surname = sur
  }
}

export default User
